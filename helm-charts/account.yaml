replicaCount: 1

app:
  name: account-name-uat1

Release:
  Maintainer: my-service
  name: service

env:
  - name: MY_ID
    value: one
  - name: ONE_ID
    value: two
  - name: NO_ID
    value: none

containerPort: 8080

image:
  repository: my-repo/my-image
  tag: latest

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"
